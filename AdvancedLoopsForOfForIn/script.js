//for of
const basket = ["apples", "oranges", "grapes"];

for (item of basket) {
  console.log(item);
}
//iterate over array and for each item of array log the item
//Iterating -- arrays, strings

//for in
const detailedBasket = {
  apples: 5,
  oranges: 10,
  grapes: 1000,
};

for (item in detailedBasket) {
  console.log(item);
}
//loop over object properties and print each property
//enumerating -- objects
